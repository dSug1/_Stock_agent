"""7_Momentum_parser — daily trade-signal screener + weekly price-change probability.

Package is named ``momentum_parser`` (the component dir ``7_Momentum_parser`` starts with a digit, so
it can't be the import name — same convention as ``platform_discoverer`` / ``hype_parser``).

Run with ``PYTHONPATH=src ../.venv/Scripts/python.exe scripts/7_momentum.py ...`` from the component dir.
"""

__version__ = "0.1.0"
