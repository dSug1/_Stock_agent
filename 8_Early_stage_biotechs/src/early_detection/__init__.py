"""early_detection — Module 8: early-detection pipeline for disruptive biotech mechanisms.

Package lives under ``src/`` and is imported as ``early_detection`` (the component directory
``8_Early_stage_biotechs`` starts with a digit, so the package is renamed — same convention as
``5_Hype_parser``→``hype_parser`` and ``6_Biotech_platform_discoverer``→``platform_discoverer``).

Phase 1 = universe module (US + Canada + Module-6 seed merge). See ``spec/phase1_universe_build_spec.md``
and ``spec/decisions.md``.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
